'use strict';

describe('Service: Workouts', function () {
  //var Workouts,
  //    httpBackend,
  //    sampleData = {
  //      data: []
  //    };
  //
  //beforeEach(module('liftbroApp'));
  //
  //beforeEach(inject(function (_Workouts_, $httpBackend) {
  //  Workouts = _Workouts_;
  //  httpBackend = $httpBackend;
  //}));
  //
  //it('should do something...', function () {
  //  expect(1).toEqual(1);
  //});

});
