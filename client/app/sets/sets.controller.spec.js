'use strict';

describe('Controller: SetsCtrl', function () {

  // load the controller's module

  beforeEach(module('liftbroApp'));

  it('should ...', function() {
    expect(1).toEqual(1);
  });
});
